# NBA 球员数据分析与机器学习项目最终汇总报告

## 1. 项目概述

本项目围绕 NBA 历史球员赛季数据，构建从原始数据清洗、探索性分析、无监督聚类、监督学习预测到 Streamlit 交互看板的完整分析流程。数据覆盖 1950 至 2017 赛季，适合观察球员能力、位置特征和比赛风格随时代的变化。

项目采用按模块分工的协作方式：角色 A 负责主数据集，角色 B 负责 EDA 与可视化，角色 C 负责聚类挖掘，角色 D 负责预测建模，角色 E 负责看板开发与最终整合。

## 2. Git 历史与交付情况

`main` 分支的提交历史记录了完整的协作顺序：

| 时间 | 合并内容 | 对应角色 | 主要交付 |
| --- | --- | --- | --- |
| 2026-09-03 | 初始仓库 | 全体 | 项目目录、依赖与协作规范 |
| 2026-09-04 至 09-05 | 数据清洗、EDA | A、B | `data_cleaning.py`、`visualization.py`、EDA Notebook |
| 2026-09-05 | 预测建模 | D | `models.py`、预测 Notebook |
| 2026-09-06 至 09-07 | 聚类挖掘 | C | 聚类 Notebook、时代图和球员画像图 |
| 2026-09-07 | 看板与整合 | E | `app/app.py`、本报告与汇报 PPT |

项目使用 `main` 分支汇总各功能分支。原始 CSV、处理后 CSV 和模型权重均通过 `.gitignore` 排除，保证仓库不提交大体量数据和二进制模型文件。

## 3. 数据工程

### 3.1 数据来源与规模

输入文件包括：

| 文件 | 内容 | 原始记录数 |
| --- | --- | ---: |
| `Players.csv` | 球员身高、体重、学校和出生地信息 | 3,922 |
| `player_data.csv` | 生涯起止年份、位置、身高体重和出生日期 | 4,550 |
| `Seasons_Stats.csv` | 球员赛季常规赛统计和高级指标 | 24,691 |

数据清洗后，主数据集 `master_data.csv` 包含 20,313 条球员赛季记录、78 个字段、3,921 名球员，覆盖 1950 至 2017 赛季。

### 3.2 清洗与特征构造

`src/data_cleaning.py` 完成以下工作：

- 删除原始导出的索引列和全空占位列。
- 统一身高、体重和出生信息的单位及字段命名。
- 对交易赛季优先保留 `TOT` 汇总行，避免重复统计。
- 使用姓名、出生年份和赛季年龄处理同名球员匹配。
- 构造 `era`、真实命中率 `TS%`、每 36 分钟统计等特征。
- 保留早期年代尚未统计的高级指标缺失值，不将其误填为 0。

## 4. 探索性分析与可视化

`src/visualization.py` 将绘图逻辑封装为可复用函数，提供描述统计、相关性热力图、得分分布、趋势图、箱线图、小提琴图和分组中位数比较。

为了减少短期出场造成的比例指标失真，项目统一采用 `MP >= 500` 且 `G >= 5` 的合格样本口径。该口径保留 14,860 条球员赛季记录，占主数据集的 73.2%。

在合格样本中，场均得分中位数为 9.4 分，PER 中位数为 13.9，TS% 中位数为 0.520，场均出场时间中位数为 24.9 分钟。三分出手比 `3PAr` 的赛季中位数从 1980 年的 0.011 增至 2017 年的 0.324，展示了联盟比赛风格向外线投射迁移的趋势。

需要注意：原始数据不含薪资，因此项目中按 BPM 构造的价值梯队仅为表现代理变量，不能解释为真实薪资分层。

## 5. 聚类挖掘

聚类模块按 1980s、1990s、2000s 和 2010s 分时代运行，并产出 PCA、t-SNE、特征雷达图和球员簇画像图。其目标不是复刻传统五个位置，而是通过技术统计识别功能类型，例如传统组织后卫、定点射手、护框吃饼中锋、攻防一体中锋和多功能球员。

项目已提交每个时代的聚类可视化结果，前端直接加载这些图片，保证角色 C 的结果可见且不复制聚类训练逻辑。

## 6. 预测建模

预测模块以“下一赛季 PER”和“下一赛季是否达到全明星级门槛”为目标，采用按时间切分的数据验证方式：训练集截至 2010 年，测试集为 2011 至 2017 年。该设计避免把未来赛季信息泄漏给训练过程。

`src/models.py` 提供统一推理接口：

- `predict_per`：预测下一赛季 PER。
- `predict_allstar_proba`：预测达到全明星级门槛的概率。
- `explain_prediction_waterfall`：输出 SHAP 瀑布图解释。

当前仓库未提交模型权重文件，因此看板会在模型缺失时给出接入提示，而不会伪造预测结果。运行预测 Notebook 的导出单元后，将模型保存为 `saved_models/nba_per_xgb_pipeline.pkl` 和 `saved_models/nba_allstar_xgb_pipeline.pkl` 即可启用页面。

## 7. Web 前端看板与整合

角色 E 实现的 `app/app.py` 使用 Streamlit 构建简洁浅色的数据分析看板，并复用角色 B 的数据加载、派生特征、样本筛选和 Matplotlib 图表函数。

看板包括六个页面：

1. 项目概览：展示记录量、球员数、赛季范围、核心中位数指标和趋势。
2. 球员与赛季探索：支持按球员查看 PER 赛季趋势和明细表。
3. 效率与分布：展示得分、效率、出场时间等分布和相关性热力图。
4. 球队与位置对比：以球队或位置聚合比较核心指标。
5. 聚类挖掘成果：加载角色 C 已生成的分时代 PCA、雷达图和时代演化图。
6. 预测模型：检测本地模型文件，存在时提供预测表单，缺失时展示接入说明。

侧边栏统一提供赛季、位置、球队和最低出场时间筛选，并用 `st.cache_data` 缓存主数据读取，避免每次交互重复读取 CSV。

启动方式：

```bash
streamlit run app/app.py
```

## 8. 局限与后续工作

- 数据截至 2017 年，不能直接反映近年 NBA 的球员与战术变化。
- 部分高级指标在早期年代为结构性缺失，应按年代解释而非零值填充。
- 当前模型文件未随仓库提交，需在本地训练并导出后启用线上推理。
- 聚类结果以静态图接入，后续可补充 `Player`、`Year`、`Cluster` 形式的结果 CSV，以支持球员级交互。
- 可补充工资帽、合同、季后赛与高阶追踪数据，扩展价值评估和预测范围。

## 9. 结论

项目已形成完整的离线分析到交互展示链路。数据层处理了多源数据、交易赛季和同名球员问题，EDA 统一了合格样本口径，聚类模块刻画了球员功能类型，预测模块预留了可解释的模型接口，看板则将这些成果集中到一个可筛选、可演示的前端入口中。
